services:
  stock-risk-agent:
    build: .
    container_name: stock_risk_agent
    restart: unless-stopped
    volumes:
      - ./portfolio.csv:/app/portfolio.csv
      - ./.env:/app/.env
    environment:
      - PYTHONUNBUFFERED=1
